<!DOCTYPE html>
<html>
<head>
    <title>Universal Web Scraper</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body class="container mt-4">
    <h1>Universal Web Scraper</h1>
    {% if error %}
    <div class="alert alert-danger">{{ error }}</div>
    {% endif %}
    <form method="POST">
        <div class="mb-3">
            <label class="form-label">Target URL</label>
            <input type="url" name="url" class="form-control" required placeholder="https://example.com">
        </div>
        <div class="mb-3">
            <label class="form-label">CSS Selector</label>
            <input type="text" name="selector" class="form-control" required placeholder="e.g., h1, .class, #id">
        </div>
        <div class="mb-3">
            <label class="form-label">Mode</label>
            <select name="mode" class="form-select">
                <option value="static">Static (faster)</option>
                <option value="js">JS-Rendered (Playwright)</option>
            </select>
        </div>
        <button type="submit" class="btn btn-primary">Scrape</button>
    </form>
</body>
</html>
